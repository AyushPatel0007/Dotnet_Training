@model IEnumerable<EntityDemo.Models.Student>

@{
    ViewBag.Title = "Display";
}

<h2>Display</h2>

<p>
    @Html.ActionLink("Create One","RegisterForm")
</p>
<script>
    
</script>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sname)
        </th>
        
        <th>
            @Html.Label("Edit")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Sname,"DisplayById", new { id = item.Sid })
        </td>
        
        <td>
            @Html.ActionLink("Edit", "RegisterForm", new { item.Sid, item.Sname, item.Semail, item.Spassword, item.Departments_Did }) |
            @Html.ActionLink("Details", "DisplayById", new { id=item.Sid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Sid })
        </td>
    </tr>
}

</table>
